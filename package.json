{
  "name": "nig-web",
  "version": "1.0.0",
  "description": "Kenshiki AI website optimized for Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev --site",
    "deploy": "wrangler deploy --site",
    "build": "echo 'No build needed'"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {}
}